# 项目说明书与规则一致性审查

## 目标

核对根 README 的架构图、大模型提示词注入、语言服务器注入及 `.agent/rules/README.md` 是否与当前代码一致，并输出不含失真事实的重绘提示词。

## 边界

- 以当前代码、配置、测试和发布产物为事实源。
- 不修改业务代码、版本号或发布产物。
- 文档中的动态数字、链路和名称必须能由代码复验。

## 阶段

- [x] 核验原图错误与重绘约束。
- [x] 核对大模型提示词注入链路。
- [x] 核对语言服务器与中文标题注入链路。
- [x] 核对项目规则文档。
- [ ] 主代理复核并交付生成提示词。

## 阶段取证

### 原图审查

- Sankey 图写死 `109` 模型，并把 Antigravity 主链路画成 ACP stdio；当前代码实际为 LS/CDP + HTTP 代理主链路。
- 雷达图包含已为空字符串的双锚点、过期 `109` 模型数字，以及无可验证依据的 100 分量化。
- 重绘图不得使用固定模型数量、失效双锚或无测试基准的能力分数。

### 代码与文档边界

- System Prompt 实际改写集中在 `SP_MODE=invert`；`custom` 配置不会单独开启请求改写。
- Antigravity 私有 Gemini 标题首行规则为 4~10 个汉字；`TITLE_ONLY_ZH_SP` 通用规则为 8~18 个汉字，测试不硬校验最终长度。
- BYOK 路由只覆盖命中的 Connect-RPC 聊天路径；模型目录注入只改变响应可见性/disabled 字段。
- 当前仓库只有 `zk-proxy-pro`；规则文档中列出的 Min、rt-flow、109 模型和 `const VERSION` 双信源均已漂移。
- 普通 `deactivate`、真卸载和手动 `zk.restoreOfficial` 的清理语义不同，不能合并描述为每次都彻底清锚。

### 用户最新 README 复核

- 新 Mermaid 双链路图和标题泳道已消除旧图的 `109`、双锚和“ACP 主对话”错误，主链路描述基本对应代码。
- 新能力矩阵图片仍引用外链，但视觉上已改为事实矩阵；Markdown 表格中 JSONC 入口应为 `extension.js`，模型目录合并入口应为 `bundled-origin/source.js`，标题入口应同时包含 `source.js` 与 `sp_invert.js`。
- 标题泳道“返回中文标题”属于结果保证过度；代码只改写标题提示词，不硬校验模型最终输出字数或语言。
- `.agent/rules/README.md` 当前仍保留不存在模块、109 模型、`const VERSION`、Node 18、错误路径和过宽 Fail-Safe 描述。

## 完成标准

- 每项结论附具体代码路径、行号或运行结果。
- 明确区分 Antigravity 主链路与兼容 ACP 链路。
- 模型数量、端口、Node 版本和锚点状态不再使用过期值。
